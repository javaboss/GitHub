name: outputs_test

on:
  workflow_dispath:

jobs:

  job1:
    runs-on: ubuntu-latest

    outputs:
      output1: ${{ steps.step1.outputs.test }}
      outpus2: ${{ steps.step2.outputs.test }}

    steps:
      - id: step1
        run echo "::set-output name=test::Hello"
      - id: step2
        run echo "::set-output name=test::World"

    